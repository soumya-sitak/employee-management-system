apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "employee-management.backend.fullname" . }}-config
data:
  DB_HOST: {{ include "employee-management.postgres.fullname" . }}-service
  DB_PORT: "5432"
  DB_NAME: employee_db
