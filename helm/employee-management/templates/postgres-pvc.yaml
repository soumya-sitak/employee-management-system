apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  name: {{ include "employee-management.postgres.fullname" . }}-pvc

spec:
  accessModes:
    - ReadWriteOnce

  resources:
    requests:
      storage: {{ .Values.postgres.storage.size }}